# Skill Mutation Draft — coding

**Created:** 2026-04-17T09:20:49.481996
**Trace:** task-20260411012034-2
**Confidence:** 0.9

---

## Root Cause
The skill failed because it was unable to execute its intended functions.

## Failing Component
It attempted to run a health check on Sig Botti systems, which required specific components (Ollama daemon and episode_logger), but these were not available or could not be accessed.

## Proposed Mutation
Replace the 'pgrep' command with a more appropriate tool for checking Ollama's status (e.g., `ps aux | grep ollama`)

## Mutation Type
replace

---

## Full Model Analysis
```json
{
  "root_cause": "The skill failed because it was unable to execute its intended functions.",
  "failing_component": "It attempted to run a health check on Sig Botti systems, which required specific components (Ollama daemon and episode_logger), but these were not available or could not be accessed.",
  "proposed_mutation": "Replace the 'pgrep' command with a more appropriate tool for checking Ollama's status (e.g., `ps aux | grep ollama`)",
  "mutation_type": "replace",
  "confidence": 0.9
}
```

---

## Original Skill Content (for reference)
```
(skill file not found)
```

---

## Instructions
- Review the proposed mutation above
- If approved, apply to: `~/.openclaw/skills/coding/SKILL.md`
- Run `python3 scripts/evaluate_variant.py` to validate before merging
- Delete this draft after applying or discarding
